# GitHub Pages 部署指南

## 📋 部署前確認

✅ **所有資料都是本地的**
- 不再從 Manus 或外部伺服器抓取圖片
- 所有圖片都存放在 `public/images/` 資料夾
- Logo 檔案都已正確配置

✅ **移除了 emoji，使用專業圖示**
- 施工流程使用 Lucide React 圖示
- 更加專業和一致的視覺風格

✅ **建築產業風格優化**
- 新增建築藍圖風格網格背景
- 專業的建材紋理效果
- 更符合建築建材產業的視覺呈現

## 🚀 部署到 GitHub Pages

### 方法一：自動部署（推薦）

1. **推送到 GitHub**
   ```bash
   git add .
   git commit -m "準備部署到 GitHub Pages"
   git push origin main
   ```

2. **啟用 GitHub Pages**
   - 前往你的 GitHub repository
   - 點擊 **Settings** → **Pages**
   - 在 **Source** 選擇 **GitHub Actions**

3. **自動建置與部署**
   - GitHub Actions 會自動執行 `.github/workflows/deploy.yml`
   - 建置完成後會自動部署到 GitHub Pages
   - 網站會在 `https://<你的用戶名>.github.io/<repository名稱>/` 上線

### 方法二：手動部署

如果你想手動部署：

```bash
# 1. 建置專案
npm run build

# 2. 進入建置資料夾
cd dist

# 3. 初始化 git（如果還沒有）
git init
git add -A
git commit -m 'deploy'

# 4. 推送到 gh-pages 分支
git push -f git@github.com:<用戶名>/<repository名稱>.git main:gh-pages

cd ..
```

## 🔧 設定說明

### Vite 配置

[vite.config.ts](vite.config.ts) 已經配置好：
- `base: '/'` - 使用根路徑（適合自訂域名）
- 如果你的 repo 不是用自訂域名，需要改成：
  ```typescript
  base: '/<repository名稱>/'
  ```

### 重要檔案

- `.github/workflows/deploy.yml` - GitHub Actions 自動部署腳本
- `public/.nojekyll` - 防止 GitHub Pages 使用 Jekyll 處理
- `dist/` - 建置輸出資料夾（不要提交到 git）

## 📝 部署檢查清單

部署前請確認：

- [ ] 所有圖片都在 `public/images/` 資料夾
- [ ] Logo 正確顯示在所有頁面
- [ ] 執行 `npm run build` 沒有錯誤
- [ ] 瀏覽器測試本地建置版本 `npm run preview`
- [ ] 確認 GitHub repository 設定正確
- [ ] 推送到 main 分支

## 🌐 自訂域名（選用）

如果要使用自訂域名：

1. 在 `public/` 資料夾創建 `CNAME` 檔案
2. 內容填入你的域名，例如：`www.178mat.com`
3. 在域名商設定 DNS：
   - A 記錄指向 GitHub Pages IP
   - 或 CNAME 指向 `<用戶名>.github.io`

## 🔍 故障排除

### 圖片無法顯示

- 確認圖片路徑使用 `/images/` 開頭
- 檢查 `public/images/` 資料夾是否包含所有圖片
- 執行 `npm run build` 後檢查 `dist/images/` 是否有圖片

### 路由 404 錯誤

- 在 `public/` 創建 `404.html`，內容與 `index.html` 相同
- 或使用 hash 路由模式（修改 wouter 設定）

### CSS 或 JS 無法載入

- 檢查 `vite.config.ts` 中的 `base` 設定
- 確認與 GitHub Pages 的 URL 路徑一致

## 📊 效能優化

已完成的優化：
- ✅ Code splitting（分割程式碼）
- ✅ Tree shaking（移除未使用的程式碼）
- ✅ 圖片壓縮和多尺寸 logo
- ✅ CSS minification
- ✅ 使用 CDN 字型

## 📞 需要協助？

如有問題，請檢查：
1. GitHub Actions 建置日誌
2. 瀏覽器開發者工具的 Console 和 Network
3. GitHub Pages 設定頁面的狀態訊息
