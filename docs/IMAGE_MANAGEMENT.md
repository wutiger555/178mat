# 圖片管理指南

## 📸 問題說明

由於專案原本是在 **Manus 平台**開發的，所有圖片實際託管在 Manus 服務器上。在本地開發時，這些圖片不存在於 `public/images/` 目錄中，導致圖片無法顯示。

### 圖片來源

- **原始網站**: https://www.178mat.com/
- **Manus 託管**: https://yijiebao-djcf4hqc.manus.space/
- **本地路徑**: `public/images/` （目前為空）

---

## 🔧 解決方案（3種選擇）

### 方案 A：使用代理配置（開發環境推薦）⭐

**優點：**
- ✅ 無需下載圖片，節省空間
- ✅ 始終顯示最新版本的圖片
- ✅ 設置簡單，立即可用

**設置方法：**

已在 `vite.config.ts` 中配置好了圖片代理：

```typescript
server: {
  proxy: {
    '/images': {
      target: 'https://yijiebao-djcf4hqc.manus.space',
      changeOrigin: true,
      secure: false,
    },
  },
}
```

**使用方法：**

```bash
# 直接啟動開發服務器
npm run dev
```

現在訪問 `http://localhost:3000`，圖片會自動從 Manus 加載！

**切換圖片源：**

如果 Manus 無法訪問，可以改用原網站：

```typescript
// 編輯 vite.config.ts
proxy: {
  '/images': {
    target: 'https://www.178mat.com',  // 改為原網站
    changeOrigin: true,
    secure: false,
  },
}
```

---

### 方案 B：下載圖片到本地（生產環境推薦）⭐⭐

**優點：**
- ✅ 完全本地化，不依賴外部服務
- ✅ 圖片加載速度快
- ✅ 適合生產部署

**缺點：**
- ⚠️ 需要手動下載圖片
- ⚠️ 圖片更新需要重新下載

**方法 1：使用自動下載腳本**

```bash
# 運行圖片下載腳本
npm run download-images
```

腳本會：
1. 自動掃描所有代碼中的圖片引用
2. 從 Manus 或原網站下載圖片
3. 保存到 `public/images/` 目錄

**方法 2：手動下載**

1. 訪問原網站：https://www.178mat.com/
2. 打開瀏覽器開發者工具（F12）
3. 切換到 Network 標籤
4. 過濾類型為 "Img"
5. 逐個保存圖片到 `public/images/` 目錄

**方法 3：從 Manus 導出**

如果您有 Manus 平台的訪問權限：
1. 登入 Manus 平台
2. 找到圖片資源管理
3. 批量導出所有圖片
4. 複製到 `public/images/` 目錄

---

### 方案 C：混合方案（最佳實踐）⭐⭐⭐

**推薦配置：**
- **開發環境**：使用代理（方案 A）
- **生產環境**：使用本地圖片（方案 B）

**設置方法：**

1. **開發時使用代理**

```bash
# 啟動開發服務器（自動使用代理）
npm run dev
```

2. **部署前下載圖片**

```bash
# 下載所有圖片
npm run download-images

# 構建生產版本
npm run build
```

3. **環境配置**

創建 `.env.development` 和 `.env.production`：

```bash
# .env.development
VITE_USE_IMAGE_PROXY=true

# .env.production
VITE_USE_IMAGE_PROXY=false
```

---

## 📋 圖片清單

目前代碼中引用的圖片：

### 基礎圖片
- `logo.png` / `logo.svg` - 網站 Logo
- `hero-mat-1.jpg` - 首頁輪播圖 1
- `hero-mat-2.jpg` - 首頁輪播圖 2
- `hero-mat-3.jpg` - 首頁輪播圖 3

### 工程實績圖片
根據 `src/data/projects.ts` 的引用，需要以下圖片：
- 台中案例圖片
- 台北案例圖片
- 高雄案例圖片
- 其他工程案例圖片

**查看完整列表：**

```bash
# 提取所有圖片引用
grep -r "/images/" src/data/ | grep -o '/images/[^"]*' | sort -u
```

---

## 🔄 圖片更新流程

### 添加新圖片

1. **準備圖片**
   - 格式：JPG 或 PNG
   - 工程實績：建議 1920×1080px 或更大
   - 產品圖片：建議 1200×800px 或更大
   - 命名：使用英文和數字，避免特殊字符

2. **上傳圖片**

```bash
# 複製圖片到 public/images/
cp your-image.jpg public/images/

# 或使用多個圖片
cp *.jpg public/images/
```

3. **更新代碼引用**

編輯 `src/data/projects.ts` 或 `src/data/products.ts`：

```typescript
{
  id: "new-project",
  title: "新案例",
  images: ["/images/your-image.jpg"],  // 添加圖片路徑
  // ...
}
```

4. **驗證**

```bash
# 重啟開發服務器
npm run dev

# 檢查圖片是否正常顯示
```

### 更新現有圖片

1. **替換文件**

```bash
# 直接替換同名文件
cp new-image.jpg public/images/hero-mat-1.jpg
```

2. **重啟服務器**

```bash
# Ctrl+C 停止
npm run dev
```

瀏覽器會自動刷新並顯示新圖片。

---

## 🚨 常見問題

### Q1: 圖片顯示 404 錯誤

**檢查清單：**
- [ ] 確認圖片文件存在於 `public/images/` 目錄
- [ ] 檢查文件名是否完全一致（區分大小寫）
- [ ] 確認路徑使用 `/images/` 而非 `images/`
- [ ] 重啟開發服務器

**解決方法：**

```bash
# 檢查文件是否存在
ls -la public/images/

# 檢查代碼中的引用
grep -r "hero-mat-1.jpg" src/

# 重啟服務器
npm run dev
```

### Q2: 代理配置不生效

**可能原因：**
- Vite 開發服務器未重啟
- 代理目標服務器無法訪問
- CORS 問題

**解決方法：**

```bash
# 1. 完全停止並重啟
Ctrl+C
npm run dev

# 2. 測試代理目標是否可訪問
curl -I https://yijiebao-djcf4hqc.manus.space/images/hero-mat-1.jpg

# 3. 如果無法訪問，切換到原網站
# 編輯 vite.config.ts，改為：
# target: 'https://www.178mat.com',
```

### Q3: 圖片下載腳本失敗

**常見錯誤：**
- `HTTP 403` - 服務器拒絕訪問
- `HTTP 404` - 圖片不存在
- `ECONNREFUSED` - 網絡連接失敗

**解決方法：**

```bash
# 1. 檢查網絡連接
ping www.178mat.com

# 2. 嘗試手動下載一個圖片測試
curl -O https://www.178mat.com/images/hero-mat-1.jpg

# 3. 如果自動下載失敗，手動下載所有圖片
# 訪問網站，打開開發者工具，保存圖片
```

### Q4: 生產環境圖片不顯示

**檢查清單：**
- [ ] 確認圖片已下載到 `public/images/`
- [ ] 運行 `npm run build` 前圖片已存在
- [ ] 檢查 `dist/images/` 目錄是否包含圖片
- [ ] 確認部署時包含了 `images` 目錄

**解決方法：**

```bash
# 1. 重新構建
rm -rf dist
npm run download-images  # 先下載圖片
npm run build

# 2. 檢查構建產物
ls -la dist/images/

# 3. 預覽構建結果
npm run preview
```

---

## 📝 最佳實踐

### 圖片命名規範

```
✅ 好的命名：
- hero-mat-1.jpg
- project-taichung-dali.jpg
- product-embedded-mat.jpg

❌ 避免：
- 圖片1.jpg（使用中文）
- image (1).jpg（包含空格）
- mat_@_#_test.jpg（特殊字符）
```

### 圖片優化

1. **壓縮圖片**

```bash
# 使用 tinypng.com 或其他工具壓縮
# 目標：保持質量的同時減小文件大小
```

2. **使用適當的格式**
   - 照片：JPG（較小文件）
   - 透明背景：PNG
   - Logo/圖標：SVG（可縮放）

3. **響應式圖片**

```typescript
// 提供多種尺寸
images: [
  "/images/project-large.jpg",    // 桌面版
  "/images/project-medium.jpg",   // 平板版
  "/images/project-small.jpg",    // 手機版
]
```

### 版本控制

**建議：**
- ✅ 小圖片（< 100KB）：提交到 Git
- ❌ 大圖片（> 1MB）：使用 `.gitignore` 排除

```bash
# .gitignore
public/images/*.jpg
public/images/*.png
!public/images/logo.svg  # 保留 Logo
```

使用 Git LFS 管理大文件：

```bash
# 安裝 Git LFS
git lfs install

# 追蹤圖片文件
git lfs track "public/images/*.jpg"
git lfs track "public/images/*.png"

# 提交
git add .gitattributes
git commit -m "Add Git LFS for images"
```

---

## 🔗 相關資源

- [Vite 代理配置文檔](https://vitejs.dev/config/server-options.html#server-proxy)
- [圖片優化工具 - TinyPNG](https://tinypng.com/)
- [圖片優化工具 - Squoosh](https://squoosh.app/)
- [Git LFS 文檔](https://git-lfs.github.com/)

---

## 📞 需要幫助？

如果您在圖片管理上遇到問題：

1. 查看 `TROUBLESHOOTING.md` 問題排查指南
2. 檢查瀏覽器開發者工具（F12）的 Network 標籤
3. 確認圖片 URL 和本地路徑是否正確

---

**最後更新**：2025 年 1 月
